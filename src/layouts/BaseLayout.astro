---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import FloatingInquiry from '../components/FloatingInquiry.astro';
import FloatingWhatsApp from '../components/FloatingWhatsApp.astro';
import FloatingFreeSample from '../components/FloatingFreeSample.astro';
import InquiryModal from '../components/InquiryModal.astro';
import '../styles/global.css';
import { DEFAULT_LANG, buildAlternates, type Lang } from '../lib/i18n';

interface Props {
  pageTitle: string;
  /** 页面可见大标题（h1）。不传则回退用 pageTitle。
      用途分离：pageTitle 服务 <title>/OG 等 SEO 标签，headingTitle 服务读者看到的 h1。 */
  headingTitle?: string;
  description?: string;
  image?: string;
  canonicalURL?: string;
  type?: 'website' | 'article';
  pubDate?: Date;
  author?: string;
  keywords?: string[];
  customCss?: string;
  customJs?: string;
  hideTitle?: boolean;
  /** 首页传入 true：桌面端 header 初始透明，滚动后变白 */
  transparentHeader?: boolean;
  /** 当前页面语言（用于 <html lang> 和导航链接前缀） */
  lang?: Lang;
  /** hreflang 多语言替代链接（自动从 URL 生成，也可手动覆盖） */
  alternates?: { lang: string; href: string }[];
}

const {
  pageTitle,
  headingTitle,
  description = "Professional automotive parts supplier - OEM quality brake pads, filters, and engine components",
  image,
  canonicalURL = new URL(Astro.url.pathname, Astro.site).href,
  type = "website",
  pubDate,
  author,
  keywords = [],
  customCss,
  customJs,
  hideTitle = false,
  transparentHeader = false,
  lang = DEFAULT_LANG,
  alternates: alternatesProp,
} = Astro.props;

// 自动生成 hreflang：手动传入 alternates 时优先用，否则从当前 URL 路径生成
const alternates = alternatesProp ?? buildAlternates(Astro.url.pathname);

const siteName = "Meiyu Aluminum";
const siteUrl = Astro.site?.toString().replace(/\/$/, '') || '';

// Only build absolute image URL if image is provided
const ogImage = image ? new URL(image, Astro.site).href : undefined;

// Google Analytics tracking ID (set PUBLIC_GA_ID in .env)
const gaId = import.meta.env.PUBLIC_GA_ID || '';

// Organization JSON-LD (global, appears on every page)
const orgJsonLd = {
  "@context": "https://schema.org",
  "@type": "Organization",
  "name": siteName,
  "url": siteUrl,
  "logo": `${siteUrl}/favicon.svg`,
  "sameAs": [
    "https://www.twitter.com/meiyualuminum",
    "https://www.linkedin.com/meiyualuminum",
    "https://www.github.com/meiyualuminum"
  ]
};
---

<html lang={lang}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <link rel="icon" type="image/x-icon" href="https://file.meiyualuminum.com/home/favicon.ico" />

    <!-- Google Analytics 4 -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-DS2EV49TRD"></script>
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-DS2EV49TRD');
    </script>

    <!-- Inter font — 异步加载，不阻塞首屏渲染 -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" media="print" onload="this.media='all'" />
    <noscript><link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" /></noscript>

    <!-- Basic SEO -->
    <title>{pageTitle} | {siteName}</title>
    <meta name="description" content={description} />
    {keywords.length > 0 && <meta name="keywords" content={keywords.join(", ")} />}
    <link rel="canonical" href={canonicalURL} />

    <!-- hreflang 多语言替代链接 -->
    {alternates.map((alt) => (
      <link rel="alternate" hreflang={alt.lang} href={new URL(alt.href, Astro.site).href} />
    ))}
    {alternates.length > 0 && (
      <link rel="alternate" hreflang="x-default" href={new URL(alternates.find((a) => a.lang === DEFAULT_LANG)?.href ?? canonicalURL, Astro.site).href} />
    )}

    <!-- ⚠️ TEMPORARY (pre-launch): keep every page out of search engine indexes.
         noindex = don't index this page; nofollow = don't follow its links.
         REMOVE this line when the site goes live. -->
    <meta name="robots" content="noindex, nofollow" />

    <!-- Open Graph -->
    <meta property="og:type" content={type} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    {ogImage && <meta property="og:image" content={ogImage} />}
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:site_name" content={siteName} />
    {pubDate && <meta property="article:published_time" content={pubDate.toISOString()} />}
    {author && <meta property="article:author" content={author} />}

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={description} />
    {ogImage && <meta name="twitter:image" content={ogImage} />}

    <!-- Organization Schema -->
    <script type="application/ld+json" set:html={JSON.stringify(orgJsonLd)} />

    <!-- Per-page custom CSS (from database) -->
    {customCss && <style set:html={customCss} />}

    <!-- Per-page head extras (e.g. resource preloading) -->
    <slot name="head" />
  </head>
  <body>
    <Header transparent={transparentHeader} lang={lang} />
    <main class="container">
      {!hideTitle && <h1>{headingTitle || pageTitle}</h1>}
      <slot />
    </main>
    <Footer lang={lang} />
    <script>
      import "../scripts/menu.js";
      import "../scripts/watermark.js";
    </script>

    <!-- Google Analytics -->
    {gaId && (
      <script is:inline>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());
        gtag('config', '{gaId}');
      </script>
    )}
    {gaId && (
      <script is:inline src={`https://www.googletagmanager.com/gtag/js?id=${gaId}`} />
    )}

    <!-- Per-page custom JS (from database, runs after DOM is ready) -->
    {customJs && <script is:inline set:html={customJs} />}

    <!-- Global floating inquiry FAB — fixed bottom-right, all pages -->
    <FloatingInquiry />

    <!-- Global floating Free Sample capsule — above WhatsApp, desktop only -->
    <FloatingFreeSample />

    <!-- Global floating WhatsApp button — above inquiry FAB -->
    <FloatingWhatsApp />

    <!-- Global inquiry modal — triggered from any [data-open-inquiry-modal] button -->
    <InquiryModal />
  </body>
</html>
